import React from 'react';
import Nav from "../components/nav/nav";

const Home: React.FC = (props: any) => (
    <React.Fragment>
        <div>Home</div>
        <Nav />
    </React.Fragment>
);

export default Home;
