import React from 'react';
import Nav from "../components/nav/nav";

const About: React.FC = () => (
    <React.Fragment>
        <div>About</div>
        <Nav />
    </React.Fragment>
);

export default About;
